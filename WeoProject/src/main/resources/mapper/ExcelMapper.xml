<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.system.blog.excel">

    <select id="list" resultType="com.system.blog.excel.ExcelVO">
        SELECT `time`, `age`, `male`, `female`
        FROM `excel`
        WHERE 1
    </select>
    
</mapper>